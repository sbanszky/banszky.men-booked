export interface NetworkStat {
  icon: React.ComponentType;
  label: string;
  value: string;
  color: string;
}

export interface MatrixConfig {
  fontSize: number;
  chars: string;
  speed: number;
  opacity: number;
}